{{#link-to 'post' post.id}}
<div class="post__container tiles col-md-4 animated fadeIn">
  <div class="row">
    <h3>{{post.title}}</h3>
    <h6>{{post.date}}</h6>
    <div class="text__area">
      <img src="{{post.image}}" alt="image" />
    </div>
  </div>
</div>
{{/link-to}}

{{yield}}
